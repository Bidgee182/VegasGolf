import StablefordVegasApp from "@/StablefordVegasApp"; // or "../StablefordVegasApp" if alias isn't working

export default function Home() {
  return (
    <main className="min-h-screen bg-gray-100 p-6">
      <StablefordVegasApp />
    </main>
  );
}
